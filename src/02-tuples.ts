// array
const prices: (number | string)[] = [1, 2, 3, 5, 'as']

// tupla
const user: [string, number, boolean] = ['juancalvo', 15, true]

// destructuracion
const [username, age] = user;
console.log(username);
console.log(age);
